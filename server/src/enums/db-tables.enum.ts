// Add table names in ascending order

export enum dbTables {
  TENANTS = 'tenants',
  PLANS = 'plans',
  SUBSCRIPTIONS = 'subscriptions',
  ARTISTS = 'artists',
  SONGS = 'songs',
  USERS = 'users',
}
